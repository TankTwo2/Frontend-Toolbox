import React from 'react';
import './ContentScript.css';

// Content script component (not rendered by default)
const ContentScript: React.FC = () => {
  return null;
};

export default ContentScript;